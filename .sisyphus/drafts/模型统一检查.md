# Draft: 确认并统一默认模型

## 已完成的修改
- 在 `app/core/llm.py` 中加入强制检查，确保任何未显式指定的模型都会被覆盖为 `openai/gpt-oss-120b`。

## 待确认事项
- 请检查项目根目录下的 `.env` 文件，确保 `DEFAULT_LLM_MODEL` 的值为 `openai/gpt-oss-120b`，或直接删除该行以使用代码强制的默认值。

## 操作步骤（建议）
1. 打开 `.env` 文件。
2. 将 `DEFAULT_LLM_MODEL=glm5`（或其他值）改为 `DEFAULT_LLM_MODEL=openai/gpt-oss-120b`。
3. 保存并重新启动项目或重新加载环境变量。
4. 重新运行 Prometheus（或其他 agent）验证模型是否已统一。

如果您已完成上述操作，请告知，以便我们继续后续的计划或验证工作。